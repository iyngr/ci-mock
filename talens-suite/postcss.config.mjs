// Tailwind CSS v4 requires using the separate @tailwindcss/postcss plugin name
// instead of referencing 'tailwindcss' directly.
export default {
    plugins: {
        '@tailwindcss/postcss': {},
    },
};
